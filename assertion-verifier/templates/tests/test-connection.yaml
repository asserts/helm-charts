apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "assertion-verifier.fullname" . }}-test-connection"
  labels:
    {{- include "assertion-verifier.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "assertion-verifier.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
