apiVersion: v1
kind: Service
metadata:
  name: {{ include "metric-generator.fullname" . }}
  labels:
    {{- include "metric-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metric-generator.selectorLabels" . | nindent 4 }}
