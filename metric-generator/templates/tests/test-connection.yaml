apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metric-generator.fullname" . }}-test-connection"
  labels:
    {{- include "metric-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metric-generator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
