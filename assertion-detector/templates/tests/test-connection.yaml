apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "assertion-detector.fullname" . }}-test-connection"
  labels:
    {{- include "assertion-detector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "assertion-detector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
