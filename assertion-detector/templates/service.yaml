apiVersion: v1
kind: Service
metadata:
  name: {{ include "assertion-detector.fullname" . }}
  labels:
    {{- include "assertion-detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assertion-detector.selectorLabels" . | nindent 4 }}
